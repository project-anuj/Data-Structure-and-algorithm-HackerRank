vector<int> compareTriplets(vector<int> a, vector<int> b) {
    vector<int> v(2,0);
    for(int i=0;i<3;i++)
    {
        if(a[i]>b[i])
        v[0]++;
        else if(b[i]>a[i])
        v[1]++;
    }
    return v;
}
